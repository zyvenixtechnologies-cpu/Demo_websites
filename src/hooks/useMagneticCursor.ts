// Thin re-export so the hook is importable under the name requested
// in the original brief, without duplicating the implementation.
export { useMagnetic as useMagneticCursor } from './useMagnetic'
